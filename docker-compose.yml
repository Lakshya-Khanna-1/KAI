services:
  kai:
    build: .
    container_name: kai
    ports:
      - "8088:8000"
    env_file:
      - .env
    volumes:
      - kai_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  kai_data:
